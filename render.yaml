services:
  shl-recommender:
    type: web
    name: shl-assessment-recommender
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: LLM_PROVIDER
        value: mock
      - key: LOG_LEVEL
        value: INFO
